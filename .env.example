# Resend API key for transactional email (contact + carrier forms).
# Local dev reads this file; production reads the same var from Cloudflare
# Pages environment variables (Production + Preview). Never commit the real key.
RESEND_API_KEY=
